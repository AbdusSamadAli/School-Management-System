version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install

  build:
    commands:
      - npm run build
      - npm run export
      - aws s3 sync out/ s3://nextjsbucket2323ts --delete

artifacts:
  files:
    - '**/*'
